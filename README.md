| Category   | Functions                                                        |
| ---------- | ---------------------------------------------------------------- |
| String     | `upper`, `lower`, `split`, `join`, `replace`, `format`, `substr` |
| List       | `length`, `concat`, `flatten`, `distinct`, `contains`, `sort`    |
| Map        | `lookup`, `merge`, `keys`, `values`, `zipmap`                    |
| Set        | `toset`, `setunion`, `setintersection`, `setsubtract`            |
| Conversion | `tolist`, `toset`, `tomap`, `tonumber`, `tostring`               |
| Network    | `cidrhost`, `cidrsubnet`, `cidrsubnets`, `cidrnetmask`           |
| Files      | `file`, `templatefile`, `fileset`, `fileexists`                  |
| Date       | `timestamp`, `timeadd`, `formatdate`                             |

